name: Apply v3 patch once

on:
  push:
    branches:
      - agent/reuters-recall-sentinel-v3
    paths:
      - .github/workflows/apply-v3-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          ref: agent/reuters-recall-sentinel-v3
          fetch-depth: 0
      - name: Apply patch
        run: python automation/scripts/patch_v3_temp.py
      - name: Commit patched files
        shell: bash
        run: |
          set -euo pipefail
          rm -f automation/scripts/patch_v3_temp.py
          rm -f .github/workflows/apply-v3-patch.yml
          rm -f .github/workflows/patch-reuters-sentinel-v3.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'Use concise Reuters security recall query'
          git push origin HEAD:agent/reuters-recall-sentinel-v3
